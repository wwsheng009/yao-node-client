{
  "name": "yao-node-client",
  "version": "1.0.0",
  "description": "create yao script with typescript",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon --watch 'src/**/*.ts' --exec 'ts-node' -r tsconfig-paths/register src/app/scripts/myscript.ts",
    "run": "pnpm run build  && ts-node -r tsconfig-paths/register src/app/scripts/myscript.ts",
    "build": "tsc && tsc-alias",
    "build:watch": "concurrently --kill-others \"tsc -w\" \"tsc-alias -w\"",
    "build:lib": "tsc --module esnext && tsc-alias",
    "build:yao": "tsc -p ./tsconfig-yao.json && tsc-alias -p ./tsconfig-yao.json && pnpm run clean:up",
    "clean:up2": "ts-node -r tsconfig-paths/register src/tools/clean_up.ts",
    "clean:up": "node dist/tools/clean_up.js",
    "esbuild": "node build.js"
  },
  "keywords": [
    "yao-client",
    "yao"
  ],
  "author": "vincentwwsheng@gmail.com",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.0.3",
    "mime": "^3.0.0",
    "sync-fetch": "^0.4.2",
    "ws": "^8.12.1"
  },
  "devDependencies": {
    "@types/mime": "^3.0.1",
    "@types/node": "^18.14.0",
    "@types/sync-fetch": "^0.4.0",
    "@types/ws": "^8.5.4",
    "concurrently": "^7.6.0",
    "esbuild": "^0.17.8",
    "nodemon": "^2.0.20",
    "ts-node": "^10.9.1",
    "tsc-alias": "^1.8.2",
    "tsconfig-paths": "^4.1.2",
    "typescript": "^4.9.5"
  }
}